// eslint-disable-next-line react/prop-types
function Title({ title }) {
  return (
    <h1 className="font-bold text-4xl capitalize tracking-wide text-zinc-800 pt-2 pb-10">
      {title}
    </h1>
  );
}

export default Title;
